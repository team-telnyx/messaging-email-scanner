# MSG-1798: Obfuscated text detection
# Catches character substitution patterns used to evade text filters
# Each symbol scores ~5.0 — contributes to aggregate score but isn't deterministic

symbol {
  OBFUSCATED_SPAM {
    score = 5.0;
    description = "Obfuscated spam text detected (character substitution pattern)";
  }
}
