# MSG-1798: IDN homograph detection
# Detects internationalized domain names using lookalike Unicode characters
# to impersonate legitimate domains (e.g., Cyrillic "аpple.com")

symbol {
  IDN_HOMOGRAPH {
    score = 8.0;
    description = "Internationalized domain name with mixed scripts (possible homograph attack)";
  }
}
