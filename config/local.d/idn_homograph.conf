# MSG-1798: IDN homograph detection
# Detects mixed-script labels and reviewed protected-brand lookalikes.
# Score remains disabled until the MSG-1791 shadow launch gate is complete.

symbol {
  IDN_HOMOGRAPH {
    score = 0.0;
    description = "Mixed-script or protected-brand IDN (possible homograph attack)";
  }
}
