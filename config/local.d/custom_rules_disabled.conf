# MSG-1798 launch gate
# Scores set to 0.0 until shadow evaluation complete.
# Enable by setting scores > 0 after MSG-1791 launch gate evaluation.
#
# The score definitions live in override.d/multimap.conf and the dedicated
# symbol files. Keep all of them at zero so a configuration include-order
# change cannot accidentally activate these rules before approval.
