# Rspamd loads antivirus engines from local.d/antivirus.conf. Keep the
# MSG-1783 ClamAV settings in their dedicated file and include them here.
.include(try=false, priority=10) "$LOCAL_CONFDIR/local.d/clamav.conf"
