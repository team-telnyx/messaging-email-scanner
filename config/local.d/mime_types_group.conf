# MSG-1848: score dangerous attachment evidence above the outbound action line.
# This local.d file is merged into the built-in mime_types group so unrelated
# MIME symbol weights remain intact.
symbols = {
  "MIME_BAD_ATTACHMENT" {
    weight = 6.5;
    description = "Invalid attachment mime type";
    one_shot = true;
  }
}
